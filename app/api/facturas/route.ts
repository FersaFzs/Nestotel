import { NextResponse } from 'next/server';

// Endpoint placeholder para facturas
export async function GET() {
  return NextResponse.json({
    message: 'Endpoint de facturas preparado para futura implementación.',
  });
}
